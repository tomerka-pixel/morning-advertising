# הפעלת יוצר המודעות (OpenAI) — מקומי ומאורח

הכלי `ads.html` מייצר מודעת תמונה אמיתית וקופי בעברית מול OpenAI.
הפרונטאנד לעולם לא מחזיק מפתח. כל קריאה ל-OpenAI יוצאת מהשרת בלבד.

## מה רץ איפה
- `api/_openai.js` — החיבור ל-OpenAI (תמונה + טקסט), משותף לשני מצבי ההרצה
- `api/generate.js` — יצירת מודעת תמונה (GPT Image 2.5)
- `api/post.js` — כתיבת הקופי
- `api/health.js` — בדיקת זמינות, מדווח אילו מנועים מחוברים
- `server.js` — השרת המקומי, מפעיל את אותו מודול כשמגיעה בקשה עם `engine: "openai"`

## שני מנועים נפרדים: טקסט ותמונה
- **טקסט (הקופי)** רץ מקומית על **המנוי של ChatGPT**, דרך ה-CLI של Codex שמגיע
  בתוך אפליקציית ChatGPT (`/Applications/ChatGPT.app/Contents/Resources/codex`).
  אין צורך במפתח ואין עלות נוספת. אפשר לעקוף עם `CODEX_BIN`.
  שתי נקודות שחייבות להישמר: להריץ מתיקייה נייטרלית (בתוך המאגר Codex מתנהג
  כסוכן וסורק קבצים), ולסגור את stdin (`stdio:['ignore',...]`) אחרת הוא ממתין
  לקלט נוסף ותוקע את הבקשה.
- **תמונה** דורשת **OPENAI_API_KEY**. המנוי של ChatGPT לא נותן גישה למנוע
  התמונות דרך ה-API, ואין דרך נתמכת לעקוף את זה.
- סדר העדיפות לטקסט: מפתח OpenAI אם קיים, אחרת Codex, אחרת ה-CLI של Claude.

## המפתח
למצב מאורח, ולתמונות בכל מצב: **OPENAI_API_KEY**, נוצר ב-platform.openai.com.

**חשוב:** המפתח לא נכנס לקוד, לא לגיט, ולא להודעות צ'אט. רק למשתני סביבה.
מומלץ לקבוע מגבלת הוצאה חודשית נמוכה על המפתח ב-OpenAI.

משתנים אופציונליים:
| משתנה | ברירת מחדל | למה |
|---|---|---|
| `OPENAI_IMAGE_MODEL` | `gpt-image-2.5-flare` | `gpt-image-2.5-sunburst` לאיכות גבוהה יותר, יקר ואיטי יותר |
| `OPENAI_TEXT_MODEL` | `gpt-5.6-terra` | מודל הקופי |
| `OPENAI_IMAGE_QUALITY` | `medium` | `low` / `medium` / `high` / `xhigh` |
| `ACCESS_CODE` | אין | קוד סודי. אם מוגדר, רק בקשות עם הקוד רשאיות לייצר |

## הרצה מקומית
```bash
export OPENAI_API_KEY="..."
node server.js
```
ואז לפתוח http://localhost:8787/ads.html

## פריסה ל-Vercel (הלינק הציבורי)
1. לחבר את המאגר ל-Vercel (import project).
2. Settings → Environment Variables: להוסיף `OPENAI_API_KEY`, ומומלץ גם `ACCESS_CODE`.
3. לפרוס. מקבלים כתובת כמו `https://morning-advertising.vercel.app`.
4. להחליף ב-`ads.html` את `REPLACE_WITH_VERCEL_URL` בכתובת הזו, ולדחוף לגיט.
   מאותו רגע הלינק של GitHub Pages מייצר באמת.

בפעם הראשונה שמייצרים מהלינק הציבורי, הדפדפן מבקש את ה-ACCESS_CODE ושומר אותו במחשב.

## אזהרת עלות
נקודות הקצה ציבוריות. בלי `ACCESS_CODE` כל מי שמוצא את הכתובת יכול לייצר על חשבון המפתח.
לכן: לקבוע `ACCESS_CODE`, לקבוע תקרת הוצאה ב-OpenAI, ולא לפרסם את הכתובת.

## היגספילד
מסלול הוידאו הישן ב-`demo.html` עדיין רץ מול היגספילד דרך ה-CLI המקומי בלבד.
יוצר המודעות לא נוגע בו ולא דורש אותו.

## נכסי מותג
הלוגו ותמונות המוצר מועלים בדפדפן, נשמרים ב-IndexedDB המקומי, ונשלחים עם כל יצירה
כתמונות ייחוס דרך `POST /v1/images/edits` (multipart). כל קובץ עובר הקטנה והמרה ל-WebP
לפני השמירה, כולל SVG שעובר רסטור, כי מנוע התמונות לא מקבל וקטור.
מגבלת גוף הבקשה הועלתה ל-25MB כדי להכיל אותן.

## פרטי העסק
במוצר האמיתי הם מגיעים מהחשבון של בעל העסק במורנינג. בדף הזה הם נשמרים מקומית
(`settings/biz`) וניתנים לעריכה, כדי שאפשר יהיה להדגים בלי חיבור למערכת.
